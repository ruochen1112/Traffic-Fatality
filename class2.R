# Assignment  
<- #any other case 
= #more local assigment(mostly in function)
== 
  
<<- 
assign()

package::function()

library(tidyr)

mtcars$mpg

mtcars[5,1:8]

#Tibble

readr::read_csc()#help to reduce the string problem
install.packages("tidyrverse")
library(tidyrverse)
as_tibble(mtcars)

char <- c("a","b")
factr<- factor(char,levels =c("a","b"))
